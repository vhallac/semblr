You are the analyzer agent.

TASK ID: task-009

TASK: Diagnose failed task-006 (review)

DESCRIPTION:
Diagnose what happened with task-006 (review). It reported FAILED with empty stdout.

Do ALL of the following and write results to `/home/vedat/work/personal/semblr/temp/diagnosis-review.md`:

1. Check git status:
```bash
git status
git log --oneline -5
```

2. Check if review output file exists:
```bash
cat temp/task-006-output.md 2>&1 || echo "FILE NOT FOUND"
```

3. Run the full verification suite:
```bash
npx tsc --noEmit 2>&1 | tail -20
npx vitest run 2>&1 | tail -20
```

4. Run project verify:
```bash
npm run verify 2>&1 | tail -50
```

5. Check acceptance criteria manually:
- Does semblr_report_phase tool exist in src/semblr.ts?
- Are all 6 phases handled?
- Is config opt-in (default off)?
- Is there a switch limit?
- Are there stream announcements?

Report complete state of the feature branch: is it ready to merge?

Complete this task using available tools. Report STATUS when finished.