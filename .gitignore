# Semblr outputs — turn files, vector index, and local eval reports
.pi/rounds/
docs/eval/*.local.json
docs/eval/*.local.md
docs/eval/golden-labels.json

# pi session data (auto-generated)
.pi/agent/sessions/

# Dependencies
node_modules/

# Local environment files and per-project direnv / Nix dev shell files
.env
.envrc
shell.nix

temp/
coverage/
.stryker-tmp/
.direnv/
CLAUDE.md

.agent/
